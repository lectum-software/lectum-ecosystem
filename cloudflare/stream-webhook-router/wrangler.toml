name = "lectum-stream-webhook-router"
main = "src/worker.mjs"
compatibility_date = "2026-09-16"
keep_vars = true
workers_dev = false

# CI/CD preserva as variaveis e segredos configurados no provider; nao grave valores aqui.
# workers.dev fica desabilitado: o endpoint publico sera somente a rota HTTPS configurada no painel.
# Nao declarar segredos, account id, zona ou URL de producao neste arquivo versionado.
